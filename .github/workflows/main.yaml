on: [push]

jobs:
  hello_world:
    runs-on: ubuntu-latest
    name: Say hello
    steps:
      - uses: actions/checkout@2
      - id: foo
        uses: actions/dependacombiner@v1
        with:
          name: 'Dylan'